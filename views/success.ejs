<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Success</title>
  <link rel="shortcut icon" href="/images/logo1.png" type="image/x-icon">
  <style>
    @font-face {
      src: url(Poppins-Regular.ttf);
      font-family: pop;
    }

    body {
      display: flex;
      justify-content: center;
      align-items: center;
      height: 100vh;
      background-image: linear-gradient(to bottom right, #85a9ff, #5edecc);
      margin: 0;
      font-family: pop;
    }

    .container {
      text-align: center;
      background-color: rgba(255, 255, 255, 0.9);
      padding: 30px;
      border-radius: 12px;
      box-shadow: 0 8px 16px rgba(0, 0, 0, 0.1);
      animation: slideIn 1s ease-out;
    }

    @keyframes slideIn {
      from {
        opacity: 0;
        transform: translateY(-20px);
      }

      to {
        opacity: 1;
        transform: translateY(0);
      }
    }

    .message {
      font-size: 24px;
      color: #28a745;
      margin-bottom: 20px;
    }

    .back-link {
      text-decoration: none;
      color: #ffffff;
      background-color: #85a9ff;
      padding: 10px 20px;
      border-radius: 5px;
      transition: background-color 0.3s ease;
    }

    .back-link:hover {
      background-color: #5edecc;
    }
  </style>
</head>

<body>
  <div class="container">
    <div class="message"><%= message %></div>
    <a class="back-link" href="/">Go Back to Home</a>
  </div>
</body>

</html>
